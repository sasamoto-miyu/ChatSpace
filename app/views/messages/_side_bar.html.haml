.sidebar_user-name
.sidebar__group-list
  - current_user.groups.each do |group|
    .sidebar__group-list-one
      = link_to group_messages_path(group) do
        .sidebar__group-list-one-name
          = group.name
          %br
        .sidebar__group-list-one-messages
          まだメッセージがありません。
          %br
    
      
.sidebar__user
  %h3.sidebar__user-name-space
    = current_user.name
  .sidebar__user-icon-space
    = link_to edit_user_path(current_user) do
      %i.fas.fa-cog
    = link_to new_group_path do
      %i.fas.fa-pen